# If frontend and backend are deployed together with Vercel Services, keep this value.
NEXT_PUBLIC_API_URL=/api

# If deploying the backend as a separate Vercel project, use its public URL instead:
# NEXT_PUBLIC_API_URL=https://your-backend-project.vercel.app